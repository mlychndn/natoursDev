const allKey = ['pharma', 'global', 'xyz', 'test'];
const storedKey = ['pharma', 'global', 'xyz'];

const newKey = allKey.filter((el) => !storedKey.includes(el));

console.log(newKey);
const str = 'Malay';
const modStr = str.toLowerCase();

console.log(modStr);
